#!/usr/bin/env python

from setuptools import setup

setup(name='bio',
      version='0.1',
      description='Bioinformatics project',
      author='Damian Goworko',
      setup_requires=['pytest-runner'],
      tests_require=['pytest']
     )
